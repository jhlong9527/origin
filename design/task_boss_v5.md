# Current independent Boss task
Owner: orientation after finishing mouse controls. Only edit scripts/combat.gd (boss logic portions), scripts/actor_visual.gd (boss pose portions), scripts/main.gd (boss trail gating), tests/boss_v5_checks.gd and tests/combat_checks.gd. Do not edit player direction logic, effects.gd, character generators or arena.
User requests: ordinary attack ALWAYS forward slash -> reverse slash -> overhead chop, last chop with overcommit crouched recovery and slow rise; sprint attack windup about 1 second sword held at shoulder height, lunge stance lowers front knee to 90 degrees then forward dash keeping split legs, one-handed forward sweep; all active dash is parryable and eye red streak (root handles effects). Ground slam standing both hands raise sword above head with blade point DOWN, charged glow then drive tip into floor, crouch, expanding wave. Add distinct states boss_thrust/boss_slam/boss_overhead so overhead cannot be confused with thrust. Keep visible timing and damage aligned, ordinary no ground warning. Update shared visual_state with boss active info for root effects, explain fields. Add meaningful tests and adapt obsolete timing tests. Root is creating character models separately. Finish bounded code and verify; no subagents.
