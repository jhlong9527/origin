Boss V5 bounded task completed.
Files written: scripts/combat.gd boss logic; scripts/actor_visual.gd boss poses; scripts/main.gd boss trail gating; tests/boss_v5_checks.gd; updated obsolete timing in tests/combat_checks.gd.
Mouse direction changes remain intact; tests/v5_input_checks.log has PASS failures=0.
Boss ordinary slash always chains strike index 0 -> 1 -> 2, durations 1.10/.94/1.72 sec (phase two 10% faster). Last strike narrow overhead and crouched recovery until 74% then rise. Each hit resets once-per-strike and direction samples start of each strike. No ground warning for any ordinary strike.
Thrust: full 1.0 sec windup, 0.32 sec dash, .45 sec recovery. All dash collision active/parryable; no damage before dash. Distinct boss_thrust pose front shin -90 degrees, shoulder chamber, split-legged sweep. boss_slam and boss_overhead distinct from thrust. Slam both hands overhead, wrist tip-down, synchronized .46 impact and held crouch.
visual_state().boss_active = { kind: boss_slash/boss_thrust/boss_slam, pose: attack/boss_overhead/boss_thrust/boss_slam, combo: 0..2, progress: normalized phase, time: seconds, active: blade trail interval, parryable: live thrust dash only, glint: starts .18 sec before thrust until dash ends, charge: slam before impact, position, direction }. effects.gd untouched; root owns red eye streak/charge effects.
Tests: tests/boss_v5_checks.log BOSS_V5_CHECKS PASS failures=0 (27 checks); tests/v5_combat_checks.log COMBAT_CHECKS PASS failures=0 (30 checks). Both ran headless Godot 4.7.2. Non-blocking environment certificate-store warning only.
Remaining important: visual validate new model wrist/hand blade-down orientation after root model import because test validates joint intent but actual geometry may change. No PCK export here.
